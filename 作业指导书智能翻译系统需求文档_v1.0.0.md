# 作业指导书智能翻译系统需求文档（RD）

版本：v1.0.0
日期：2026-04-27

---

# 1. 文档概述

## 1.1 文档目的

本文档明确作业指导书智能翻译系统的开发需求、功能边界、技术规范、接口定义及验收标准，为开发人员提供清晰的开发依据，确保开发成果符合产品预期。

## 1.2 适用范围

本文档适用于本系统所有开发人员、测试人员，作为开发、调试、测试及验收的核心依据。

## 1.3 核心目标

1. **学习历史译法**：导入历史双语作业指导书文件（PDF、Word、Excel等），自动提取术语（TB）和句段（TM），建立私有翻译记忆库；
2. **智能翻译新文件**：导入全英文作业指导书（Word、Excel），系统参考记忆库和术语库进行翻译，将中文译文插入到源文件的对应位置，保留原有排版格式；
3. **术语强制一致**：翻译过程中必须严格使用术语库中已定义的译法，确保同一术语在全文中翻译一致；
4. **全程本地运行**：支持本地LLM部署，所有数据私有化存储，无外部数据泄露风险；
5. **格式保留**：Word、Excel翻译后保留原有排版、表格、样式，无需用户手动调整。

## 1.4 开发环境要求

1. 开发语言：Python 3.10+
2. 运行环境：Windows 10/11（优先）、Linux（可选）
3. 依赖管理：pip + requirements.txt
4. 编码：强制 UTF-8

---

# 2. 产品整体架构

## 2.1 架构图

```
+-------------------+      +-------------------+      +-------------------+
|   用户交互层       | ---> |   文件解析模块     | ---> |   数据导入模块     |
|  (命令行/CLI)     |      | (Word/Excel/PDF)  |      | (清洗/去重/结构化) |
+-------------------+      +-------------------+      +-------------------+
                                                              |
                                                              v
+-------------------+      +-------------------+      +-------------------+
|   文件导出模块     | <--- |   LLM翻译模块      | <--- |   检索引擎模块     |
| (Word/Excel回写)  |      | (本地/云端LLM)    |      | (TB精确匹配+TM相似)|
+-------------------+      +-------------------+      +-------------------+
                                                              ^
                                                              |
+-------------------+      +-------------------+      +-------------------+
|   配置管理模块     | <--- |   数据存储模块     | <--- |   LLM术语提取模块  |
| (参数/LLM设置)    |      | (SQLite/Chroma)   |      | (从双语文件提取TB) |
+-------------------+      +-------------------+      +-------------------+
```

## 2.2 模块职责

| 模块 | 职责 |
|------|------|
| 用户交互层 | 提供命令行操作入口，支持文件导入、翻译、术语管理、配置、导出等命令 |
| 文件解析模块 | 解析Word(.docx)、Excel(.xlsx)、PDF等格式，提取文本内容及排版结构信息 |
| 数据导入模块 | 对解析后的数据进行清洗（去空、去重、标准化），区分原文/译文，构建结构化数据 |
| LLM术语提取模块 | 调用LLM从历史双语文件中智能提取术语对（原文术语→译文术语），支持人工校验修正 |
| 检索引擎模块 | 术语库精确匹配（强制约束）+ 翻译记忆库相似句段检索（参考约束） |
| LLM翻译模块 | 调用LLM生成译文，传入术语约束和相似句段参考，确保术语一致、风格统一 |
| 文件导出模块 | 将译文写回源文件对应位置，保留Word/Excel的原有排版、样式、表格结构 |
| 数据存储模块 | 使用SQLite存储术语库、记忆库、配置；使用Chroma存储句段向量用于相似检索 |
| 配置管理模块 | 管理LLM配置（本地/云端、模型路径、API密钥）、匹配阈值、输出格式等参数 |

---

# 3. 详细功能需求

## 3.1 文件解析模块

### 3.1.1 支持格式

| 文件类型 | 学习阶段（双语文件） | 翻译阶段（待译文件） |
|----------|----------------------|----------------------|
| Word (.docx) | 支持 | 支持 |
| Excel (.xlsx) | 支持 | 支持 |
| PDF (.pdf) | 支持（仅文本提取） | 不支持（PDF为版式固定格式，无法可靠回写） |
| TXT (.txt) | 支持 | 支持 |
| CSV (.csv) | 支持 | 支持 |

> **说明**：PDF文件仅在学习阶段用于提取文本构建TM/TB，翻译阶段不支持PDF导出。如需翻译PDF内容，建议先转换为Word。

### 3.1.2 Word解析

1. 提取段落文本、表格内容、页眉页脚
2. 记录每个文本块的排版信息：字体、字号、加粗、颜色、段落样式（标题/正文/列表等）
3. 记录文本块在文档中的位置索引（段落序号、表格坐标等），用于翻译后回写
4. 支持批量解析多个Word文件

### 3.1.3 Excel解析

1. 提取所有单元格文本内容
2. 记录单元格坐标（Sheet名、行号、列号）
3. 记录单元格样式信息：字体、颜色、合并单元格、边框等
4. 支持批量解析多个Excel文件

### 3.1.4 PDF解析

1. 提取可编辑PDF中的文本内容
2. 按页提取，记录页码信息
3. 处理换行、分页符
4. **注意**：PDF仅用于学习阶段提取双语内容，不支持翻译后回写PDF

## 3.2 数据导入模块

### 3.2.1 数据清洗

1. **去空**：去除空白行、仅包含空格/制表符的行
2. **去重**：原文重复时保留最新一条译文
3. **标准化**：统一编码为UTF-8，去除多余空格，统一换行符为`\n`
4. **过滤**：过滤过短（<3个字符）和无意义的文本片段

### 3.2.2 双语对齐

1. **Word双语文件**：支持两种常见排版：
   - 段落对照：奇数段原文，偶数段译文
   - 表格对照：两列表格，左列原文右列译文
2. **Excel双语文件**：支持两列对照（A列原文，B列译文）
3. **TXT/CSV双语文件**：每行格式：`原文\t译文`
4. 自动检测对齐方式，检测失败时提示用户手动指定

## 3.3 LLM术语提取模块

### 3.3.1 核心功能

1. 将历史双语文件中的句段对传入LLM
2. LLM从中识别并提取专业术语对（英文术语 → 中文译法）
3. 支持多轮提取，确保覆盖全面
4. 提取结果支持用户人工校验、增删改

### 3.3.2 提示词模板

```
你是一名专业的技术翻译术语提取专家。请从以下双语句段中提取专业术语及其中文译法。

要求：
1. 只提取具有专业含义的词汇、短语，如设备名称、工艺参数、操作动作、材料名称等
2. 忽略普通常用词（如 the, and, please, 请, 的 等）
3. 术语以英文原文为key，中文译法为value
4. 如果同一个英文术语有多个可能译法，选择最符合上下文的一个
5. 输出格式：JSON对象，{"英文术语": "中文译法", ...}

双语句段：
{句段对列表}

请直接输出JSON，不要包含其他解释文字。
```

### 3.3.3 术语库管理

1. **查看**：`term -list` 列出所有术语
2. **添加**：`term -add "英文术语" "中文译法"`
3. **删除**：`term -del "英文术语"`
4. **修改**：`term -edit "英文术语" "新中文译法"`
5. **导入/导出**：支持从CSV文件批量导入/导出术语库

## 3.4 检索引擎模块

### 3.4.1 术语库（TB）检索

1. **精确匹配**：对待翻译文本进行分词，匹配术语库中所有包含的术语
2. **大小写不敏感**：术语匹配不区分大小写
3. **最长匹配优先**：如术语库同时有 "screw" 和 "set screw"，优先匹配 "set screw"
4. **返回值**：`{术语原文: 术语译法}` 字典

### 3.4.2 翻译记忆库（TM）检索

1. **向量化**：使用 sentence-transformers 将历史原文编码为向量，存入Chroma
2. **相似检索**：对待翻译句子进行向量检索，返回Top-K（默认3条）相似句段
3. **匹配度计算**：返回余弦相似度分数
4. **阈值控制**：
   - 匹配度 ≥ 阈值（默认0.85）：直接复用历史译文（可配置）
   - 匹配度 < 阈值：作为参考传入LLM提示词

## 3.5 LLM翻译模块

### 3.5.1 LLM接入方式

1. **本地LLM（优先）**：
   - 适配格式：GGUF（llama.cpp）、HuggingFace Transformers
   - 推荐模型：Qwen2.5、DeepSeek、Llama3 等开源模型
   - 配置参数：模型路径、上下文长度、温度、GPU/CPU切换
2. **云端LLM API（可选）**：
   - 支持OpenAI兼容格式API
   - 配置参数：API地址、API密钥、模型名称
3. **切换方式**：通过配置文件 `config.json` 修改参数即可切换，无需改代码

### 3.5.2 翻译提示词模板

```
你是一名专业的技术文档翻译专家，擅长翻译制造业作业指导书。

【待翻译文本】
{待翻译原文}

【强制术语约束】（以下术语必须严格使用指定译法，不允许修改或替换）：
{检索到的术语列表，格式：英文术语 -> 中文译法}

【参考历史译法】（以下是与当前句子相似的历史翻译，请保持风格一致）：
{检索到的TM句段列表，格式：原文 -> 历史译文（匹配度：XX%）}

【翻译要求】
1. 必须严格使用上述强制术语约束中的译法，不得替换为其他译法
2. 参考历史译法的风格，保持译文专业、准确、简洁
3. 忠于原文含义，不添加原文没有的内容，不遗漏原文信息
4. 译文自然流畅，符合中文技术文档表达习惯
5. 只输出译文内容，不要输出任何解释、说明、标记

请翻译：
```

### 3.5.3 译文后处理

1. **术语校验**：检查译文中是否包含术语库中定义的中文译法，若LLM未使用则自动替换
2. **格式保留**：保留原文中的换行、空格、特殊符号位置
3. **空值处理**：翻译失败时保留原文并标记，不中断整体流程

## 3.6 文件导出模块

### 3.6.1 Word导出

1. 将译文写回原段落/表格单元格位置
2. **保留原有排版**：
   - 保留原文本的字体、字号、颜色、加粗/斜体等格式
   - 保留段落样式（标题级别、列表编号等）
   - 保留表格结构、合并单元格、边框
3. **双语输出模式（默认）**：在原文下方或右侧插入译文，保留原文
4. **仅译文模式**：可选仅保留译文，删除原文
5. 输出文件名自动添加后缀，如 `原文件名_中文.docx`

### 3.6.2 Excel导出

1. 将译文写回原单元格位置
2. **保留原有排版**：
   - 保留单元格字体、颜色、边框
   - 保留合并单元格
   - 保留列宽、行高
3. **双语输出模式（默认）**：在原文单元格内换行追加译文，或写入相邻列
4. **仅译文模式**：可选仅保留译文
5. 输出文件名自动添加后缀，如 `原文件名_中文.xlsx`

### 3.6.3 其他格式导出

1. **TXT/CSV**：直接输出译文内容，保持原有行结构
2. **PDF**：不支持直接回写PDF，如需PDF输出，建议先翻译为Word再转换

## 3.7 数据存储模块

### 3.7.1 存储方案

| 数据类型 | 存储方式 | 说明 |
|----------|----------|------|
| 术语库（TB） | SQLite | 表结构：id, source_term, target_term, created_at, source_file |
| 翻译记忆库（TM） | SQLite + Chroma | SQLite存原文/译文/元数据；Chroma存向量用于相似检索 |
| 用户配置 | SQLite | 表结构：key, value |
| 翻译记录 | SQLite | 原文、译文、使用术语、翻译时间、源文件 |

### 3.7.2 数据管理

1. **备份**：`backup` 命令将SQLite数据库和Chroma数据打包为zip
2. **恢复**：`restore [备份文件路径]` 恢复数据
3. **清理**：`clean -tm` 清理记忆库 / `clean -tb` 清理术语库
4. **统计**：`stats` 查看术语数量、记忆库句段数量、翻译记录数量

## 3.8 用户交互模块（命令行版）

### 3.8.1 命令列表

| 命令 | 功能 | 示例 |
|------|------|------|
| `import -t <文件路径>` | 导入历史双语文件，提取TM/TB | `import -t ./data/bilingual.docx` |
| `import -b <文件夹路径>` | 批量导入文件夹内所有双语文件 | `import -b ./data/history/` |
| `term -list` | 查看术语库 | - |
| `term -add <英文> <中文>` | 添加术语 | `term -add "torque wrench" "扭力扳手"` |
| `term -del <英文>` | 删除术语 | `term -del "torque wrench"` |
| `term -edit <英文> <新中文>` | 修改术语 | `term -edit "screw" "螺钉"` |
| `term -import <csv路径>` | 从CSV批量导入术语 | `term -import ./terms.csv` |
| `term -export <csv路径>` | 导出术语库到CSV | `term -export ./terms.csv` |
| `translate -f <文件路径> -o <输出路径>` | 翻译文件 | `translate -f ./doc.docx -o ./doc_cn.docx` |
| `config -set <参数> <值>` | 设置配置 | `config -set llm_type local` |
| `config -get <参数>` | 查看配置 | `config -get llm_type` |
| `stats` | 查看统计信息 | - |
| `backup` | 备份数据 | - |
| `restore <路径>` | 恢复数据 | `restore ./backup_20260101.zip` |
| `help` | 查看帮助 | - |
| `exit` | 退出系统 | - |

### 3.8.2 配置文件（config.json）

```json
{
  "llm": {
    "type": "local",
    "local_model_path": "./models/qwen2.5-7b.gguf",
    "context_length": 4096,
    "temperature": 0.3,
    "gpu_layers": 0
  },
  "api": {
    "base_url": "https://api.openai.com/v1",
    "api_key": "",
    "model_name": "gpt-4"
  },
  "translation": {
    "tm_threshold": 0.85,
    "tm_top_k": 3,
    "output_mode": "bilingual",
    "batch_size": 10
  },
  "storage": {
    "db_path": "./data/trans_guide.db",
    "chroma_path": "./data/chroma_db"
  }
}
```

---

# 4. 技术栈

## 4.1 核心依赖

| 用途 | 库名 | 说明 |
|------|------|------|
| Word解析/导出 | `python-docx` | .docx文件读写 |
| Excel解析/导出 | `openpyxl` | .xlsx文件读写 |
| PDF解析 | `PyMuPDF (fitz)` | PDF文本提取 |
| 向量编码 | `sentence-transformers` | 文本向量化 |
| 向量检索 | `chromadb` | 向量存储与相似检索 |
| 本地LLM | `llama-cpp-python` | GGUF模型推理 |
| 云端LLM | `openai` | OpenAI兼容API |
| 数据存储 | `sqlite3` | 内置，无需安装 |
| 命令行 | `argparse` | 内置 |
| 进度条 | `tqdm` | 显示翻译进度 |
| 日志 | `logging` | 内置 |

## 4.2 技术规范

1. **代码规范**：遵循PEP 8，函数/变量命名使用英文小写+下划线
2. **异常处理**：文件不存在、格式错误、LLM调用失败等场景需捕获异常并给出明确错误提示
3. **日志记录**：记录文件导入、翻译操作、错误信息，日志路径可配置
4. **可扩展性**：模块化设计，预留Web界面、更多文件格式、其他LLM的扩展接口
5. **性能优化**：批量翻译使用批处理，减少LLM调用次数；向量化使用缓存

---

# 5. 接口定义

## 5.1 文件解析接口

### parse_word_file

```python
def parse_word_file(file_path: str) -> Tuple[List[Dict], Dict]:
    """
    解析Word文件
    
    Args:
        file_path: 文件路径
        
    Returns:
        texts: 文本块列表，每个元素为 {
            "text": str,
            "type": "paragraph" | "table_cell" | "header" | "footer",
            "style": str,  # 段落样式名
            "position": Dict,  # 位置信息
            "format": Dict  # 格式信息
        }
        format_info: 文档整体格式信息
        
    Raises:
        FileNotFoundError: 文件不存在
        ValueError: 文件格式错误或损坏
    """
```

### parse_excel_file

```python
def parse_excel_file(file_path: str) -> Tuple[List[Dict], Dict]:
    """
    解析Excel文件
    
    Args:
        file_path: 文件路径
        
    Returns:
        texts: 单元格文本列表，每个元素为 {
            "text": str,
            "sheet": str,
            "row": int,
            "col": int,
            "format": Dict  # 单元格格式信息
        }
        format_info: 工作簿整体格式信息
    """
```

### parse_pdf_file

```python
def parse_pdf_file(file_path: str) -> Tuple[List[Dict], Dict]:
    """
    解析PDF文件（仅文本提取）
    
    Args:
        file_path: 文件路径
        
    Returns:
        texts: 文本块列表，每个元素为 {
            "text": str,
            "page": int,
            "bbox": Tuple[float, float, float, float]
        }
        format_info: 文档页数等信息
    """
```

## 5.2 数据导入接口

### align_bilingual_data

```python
def align_bilingual_data(
    original_list: List[str],
    translation_list: List[str],
    align_mode: str = "auto"
) -> List[Tuple[str, str]]:
    """
    对齐原文和译文
    
    Args:
        original_list: 原文列表
        translation_list: 译文列表
        align_mode: 对齐方式，"auto" | "paragraph" | "table" | "line"
        
    Returns:
        对齐后的句段对列表 [(原文, 译文), ...]
    """
```

### clean_text

```python
def clean_text(text: str) -> str:
    """
    清洗文本
    
    Args:
        text: 原始文本
        
    Returns:
        清洗后的文本
    """
```

## 5.3 LLM术语提取接口

### extract_terms_by_llm

```python
def extract_terms_by_llm(
    bilingual_pairs: List[Tuple[str, str]],
    llm_config: Dict
) -> Dict[str, str]:
    """
    使用LLM从双语句段中提取术语
    
    Args:
        bilingual_pairs: 双语句段对列表
        llm_config: LLM配置
        
    Returns:
        术语字典 {英文术语: 中文译法}
    """
```

## 5.4 检索引擎接口

### search_terms

```python
def search_terms(text: str, term_db: Dict[str, str]) -> Dict[str, str]:
    """
    检索文本中包含的术语
    
    Args:
        text: 待翻译文本
        term_db: 术语库
        
    Returns:
        匹配到的术语 {术语原文: 术语译法}
    """
```

### search_tm

```python
def search_tm(
    text: str,
    tm_collection: chromadb.Collection,
    top_k: int = 3,
    threshold: float = 0.85
) -> List[Dict]:
    """
    检索翻译记忆库中的相似句段
    
    Args:
        text: 待翻译文本
        tm_collection: Chroma集合
        top_k: 返回条数
        threshold: 匹配度阈值
        
    Returns:
        相似句段列表 [{"original": str, "translation": str, "score": float}, ...]
    """
```

## 5.5 LLM翻译接口

### llm_translate

```python
def llm_translate(
    text: str,
    terms: Dict[str, str],
    tm_results: List[Dict],
    llm_config: Dict
) -> str:
    """
    调用LLM翻译文本
    
    Args:
        text: 待翻译原文
        terms: 检索到的术语
        tm_results: 检索到的相似句段
        llm_config: LLM配置
        
    Returns:
        译文
        
    Raises:
        ConnectionError: API调用失败
        RuntimeError: 本地模型推理失败
    """
```

## 5.6 文件导出接口

### export_word

```python
def export_word(
    original_path: str,
    translations: List[Tuple[int, str]],
    output_path: str,
    mode: str = "bilingual"
) -> bool:
    """
    导出翻译后的Word文件
    
    Args:
        original_path: 原文件路径
        translations: 翻译结果列表 [(位置索引, 译文), ...]
        output_path: 输出路径
        mode: "bilingual"（双语）| "target_only"（仅译文）
        
    Returns:
        是否成功
    """
```

### export_excel

```python
def export_excel(
    original_path: str,
    translations: List[Tuple[str, int, int, str]],
    output_path: str,
    mode: str = "bilingual"
) -> bool:
    """
    导出翻译后的Excel文件
    
    Args:
        original_path: 原文件路径
        translations: 翻译结果列表 [(sheet名, 行, 列, 译文), ...]
        output_path: 输出路径
        mode: "bilingual" | "target_only"
        
    Returns:
        是否成功
    """
```

## 5.7 数据存储接口

### TermDatabase

```python
class TermDatabase:
    def add_term(self, source: str, target: str, source_file: str = "") -> bool: ...
    def get_term(self, source: str) -> Optional[str]: ...
    def get_all_terms(self) -> Dict[str, str]: ...
    def delete_term(self, source: str) -> bool: ...
    def update_term(self, source: str, target: str) -> bool: ...
    def import_from_csv(self, csv_path: str) -> int: ...
    def export_to_csv(self, csv_path: str) -> bool: ...
```

### TMDatabase

```python
class TMDatabase:
    def add_segment(self, original: str, translation: str, source_file: str = "") -> bool: ...
    def search_similar(self, text: str, top_k: int = 3, threshold: float = 0.85) -> List[Dict]: ...
    def delete_by_file(self, source_file: str) -> int: ...
    def get_stats(self) -> Dict: ...
```

---

# 6. 验收标准

## 6.1 功能验收

| 验收项 | 标准 |
|--------|------|
| 文件解析 | Word/Excel/PDF/TXT/CSV文件可正常解析，无乱码、无内容缺失 |
| 双语对齐 | 历史双语文件可正确对齐原文和译文，准确率 ≥ 95% |
| 术语提取 | LLM提取的术语准确率 ≥ 80%，支持人工修正 |
| 术语检索 | 待译文本中的术语可100%匹配，无遗漏 |
| 术语约束 | 译文中术语使用与术语库一致率 = 100% |
| TM检索 | 相似句段检索准确率 ≥ 90% |
| LLM翻译 | 译文忠于原文、流畅自然、术语正确 |
| Word导出 | 译文正确插入，保留原有字体、样式、表格结构 |
| Excel导出 | 译文正确插入，保留原有单元格格式、合并单元格 |
| 数据存储 | 术语库、记忆库可正常增删改查，备份恢复功能正常 |
| 命令行 | 所有命令可正常执行，提示清晰，异常处理完善 |

## 6.2 性能验收

| 验收项 | 标准 |
|--------|------|
| 文件解析 | 单文件（≤50页/1000句）≤ 10秒 |
| 术语检索 | 单句 ≤ 0.1秒 |
| TM检索 | 单句 ≤ 1秒 |
| LLM翻译 | 单句 ≤ 5秒（本地模型，取决于硬件）|
| 批量翻译 | 100句 ≤ 60秒 |
| 稳定性 | 连续运行2小时无崩溃、无内存泄漏 |

## 6.3 兼容性验收

| 验收项 | 标准 |
|--------|------|
| 系统兼容 | Windows 10/11、Linux可正常运行 |
| Word兼容 | .docx文件可正常解析和导出 |
| Excel兼容 | .xlsx文件可正常解析和导出 |
| 依赖兼容 | requirements.txt可完整复现环境 |

---

# 7. 开发进度规划

| 阶段 | 核心任务 | 预计耗时 | 交付物 |
|------|----------|----------|--------|
| 阶段1：基础框架 | 搭建项目结构，实现配置管理、日志、命令行框架 | 0.5天 | 可运行的CLI框架 |
| 阶段2：数据存储 | 实现SQLite/Chroma存储，TermDatabase/TMDatabase | 0.5天 | 可增删改查的TB/TM |
| 阶段3：文件解析 | 实现Word/Excel/PDF/TXT/CSV解析 | 1天 | 多格式文件解析器 |
| 阶段4：数据导入 | 实现数据清洗、双语对齐、批量导入 | 0.5天 | 可导入历史文件生成TM/TB |
| 阶段5：LLM术语提取 | 实现LLM术语提取、术语库管理命令 | 0.5天 | 可自动提取+人工管理术语 |
| 阶段6：检索引擎 | 实现TB精确匹配、TM向量检索 | 0.5天 | 可检索术语和相似句段 |
| 阶段7：LLM翻译 | 实现LLM接入、提示词模板、翻译流程 | 0.5天 | 可调用LLM生成译文 |
| 阶段8：文件导出 | 实现Word/Excel译文回写，保留格式 | 1天 | 可导出保留排版的译文文件 |
| 阶段9：集成测试 | 端到端测试、性能优化、bug修复 | 1天 | 稳定可用的系统 |

**总计：约6天**

---

# 8. 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| PDF回写格式错乱 | 高 | 翻译阶段不支持PDF，仅在学习阶段提取文本 |
| 本地LLM推理速度慢 | 中 | 支持云端API切换；批量翻译使用批处理 |
| LLM术语提取不准确 | 中 | 提取后必须人工校验；提供术语导入/导出功能 |
| Word复杂样式丢失 | 中 | 明确支持范围，复杂样式（文本框、艺术字等）不保证 |
| 双语文件对齐失败 | 低 | 提供多种对齐方式选择，失败时提示用户手动指定 |

---

# 9. 附录

## 9.1 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| 翻译记忆库 | TM (Translation Memory) | 存储历史原文和译文的数据库 |
| 术语库 | TB (Termbase) | 存储专业术语及其中文译法的数据库 |
| 作业指导书 | Work Instruction | 制造业指导操作步骤的技术文档 |
| RAG | Retrieval-Augmented Generation | 检索增强生成 |
| LLM | Large Language Model | 大语言模型 |

## 9.2 目录结构建议

```
Trans_Guide_AI/
├── main.py                  # 入口文件
├── config.json              # 配置文件
├── requirements.txt         # 依赖列表
├── data/                    # 数据目录
│   ├── trans_guide.db       # SQLite数据库
│   └── chroma_db/           # Chroma向量库
├── models/                  # 本地模型存放目录
├── modules/                 # 核心模块
│   ├── __init__.py
│   ├── cli.py               # 命令行交互
│   ├── config_manager.py    # 配置管理
│   ├── file_parser.py       # 文件解析
│   ├── data_importer.py     # 数据导入与清洗
│   ├── term_extractor.py    # LLM术语提取
│   ├── term_db.py           # 术语库操作
│   ├── tm_db.py             # 记忆库操作
│   ├── retriever.py         # 检索引擎
│   ├── llm_client.py        # LLM客户端
│   ├── translator.py        # 翻译流程编排
│   ├── file_exporter.py     # 文件导出
│   └── logger.py            # 日志
├── tests/                   # 测试用例
└── docs/                    # 文档
```
