彻底优化统计面板页面，必须100%对齐系统现有Linear深色风格（参考：用户管理/文本翻译/翻译历史页面），重点解决突兀emoji、排版混乱、风格割裂问题，要求如下：

一、整体规范强制对齐（所有模块必须遵守）
1. 页面边距：与用户管理页面完全一致，左右24px、上下24px，标题区与内容区间距16px，模块之间间距24px
2. 字体层级：严格复用系统现有层级
   - 页面主标题：18px #e4e4e7 字重600
   - 模块标题（使用趋势/详细数据）：16px #e4e4e7 字重500
   - 正文/次要文字：14px #a1a1aa
   - 数据突出数字：24px #e4e4e7 字重700
3. 色彩规范：所有元素必须使用系统CSS变量（--primary/--card-bg/--border-primary等），禁止硬编码色值，与现有页面保持统一
4. 交互规范：所有按钮/卡片/控件的hover反馈、点击效果，必须与系统现有元素完全一致（如按钮hover背景#18181b、卡片hover阴影加深）

二、顶部标题区优化（对齐其他页面标题样式）
1. 标题「统计面板」+ 副标题「查看系统使用情况和数据统计」，排版与翻译历史页面标题完全一致
2. 右侧「刷新」按钮：复用系统次要按钮样式，背景透明、边框1px solid #27272a，hover背景#18181b，文字14px #a1a1aa，与翻译历史的「下载/删除」按钮风格统一

三、顶部概览卡片优化（重点解决emoji问题）
1. 【核心：移除所有突兀emoji】，替换为系统统一的线性图标（与侧边栏图标风格一致，如用户用👤线性图标、文件用📄线性图标、术语用📚线性图标、LLM用🤖线性图标，禁止使用彩色/花哨emoji）
2. 卡片样式复用系统数据卡片规范：
   - 背景#18181b、边框1px solid #27272a、圆角8px，hover效果与用户管理页面卡片一致
   - 四个卡片宽度必须等宽，间距16px，布局整齐不歪斜
3. 卡片内排版强制对齐：
   - 左侧：线性图标（18px，#a1a1aa），与数字垂直居中对齐
   - 中间：突出数字（24px粗体），下方两行小字（标题+今日数据，14px #a1a1aa），整体居中对齐
   - 禁止图标/数字/文字错位、间距过大，保持紧凑有呼吸感，参考系统其他数据卡片的排版逻辑

四、使用趋势图表区优化
1. 模块标题「使用趋势」与右侧时间筛选按钮排版对齐，禁止标题偏左、按钮偏右的松散布局
2. 时间筛选按钮（7天/30天/90天）：复用文本翻译页面的语言切换器样式，改为系统统一的分段切换器：
   - 背景#18181b、内边距2px、圆角6px，选中态背景#3b82f6、文字#ffffff，未选中态背景透明、文字#a1a1aa，hover反馈与系统按钮一致
3. 图表容器：背景#18181b、边框1px solid #27272a、圆角8px，与卡片样式统一
4. 图表配置：强制复用系统统一的Chart.js配置（参考系统现有图表样式），禁止自定义颜色/字体/网格线：
   - 翻译文件数：使用系统品牌蓝#3b82f6，LLM调用使用系统辅助紫#9b59b6
   - 网格线/坐标轴文字/图例/hover提示框，必须与系统现有图表样式完全一致，禁止出现突兀的浅色边框/文字

五、详细数据区优化
1. 模块标题「详细数据」与右侧标签页排版对齐，禁止标题偏左、按钮偏右
2. 标签页（用户排行/文件记录/LLM记录）：复用系统现有标签页样式（参考翻译记忆页面的标签切换器），改为文字下划线高亮模式，选中态文字#3b82f6+底部2px同色下划线，未选中态文字#a1a1aa，禁止使用蓝色填充按钮，与系统其他标签页风格统一
3. 数据表格：100%复用用户管理页面的表格样式，包括表头背景、文字颜色、行高、hover效果、边框样式，禁止自定义表格样式，与用户管理表格保持完全一致

六、最终验收标准
1. 统计面板的视觉风格、交互逻辑、组件形态，必须与用户管理/文本翻译/翻译历史页面无割裂感，相似度≥95%
2. 无任何突兀的emoji、自定义色值、非系统组件
3. 所有排版整齐、间距合理，无错位/歪斜/松散问题